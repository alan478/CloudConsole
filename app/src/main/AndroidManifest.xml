<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cloudconsole">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloudConsole"
        android:usesCleartextTraffic="true">
        <activity android:name=".IAMUserActivities.IamUserDashBoard"></activity>
        <activity android:name=".RootActivities.RootUserDashBoard" />
        <activity android:name=".RootActivities.RegisterRootUser" />
        <activity android:name=".IAMUserActivities.IAMUser" />
        <activity android:name=".RootActivities.RootUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>